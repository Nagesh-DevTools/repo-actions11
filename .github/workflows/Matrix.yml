name: Test with fail-fast and max-parallel

on:
  push:

jobs:
  test:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        python-version: [3.2,3.4]
        files: [pyrmaid.py , helloworld.py]
      fail-fast: false         
      max-parallel: 2    
    steps:
      - uses: actions/checkout@v4

      - name: Set up Python ${{ matrix.python-version }}
        uses: actions/setup-python@v5
        with:
          python-version: ${{ matrix.python-version }}

      - name: Run tests
        run: |
            python ${{ matrix.files }}
